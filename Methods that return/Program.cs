using System;

namespace Methods_that_return
{
    class Program
    {
        static void Main()
            ////{
            ////    int resultFromAddition = Adittion(Adittion(1, 2), Adittion(5, 6));
            ////    Console.WriteLine(resultFromAddition);
            ////}

            ////static int Adittion(int firstNumber, int secondNumber)
            ////{
            ////    int result = firstNumber + secondNumber;
            ////    return result; 
            ////}
            //{ 
            //    Console.WriteLine($"");
            //}
        //{
        //    string rezultat1 = SortThreeNumbers(4, 5, 8);
        //    string rezultat2 = SortThreeNumbers(6, 23, 1);
        //    Console.WriteLine($"{rezultat1} ; {rezultat2}");
            
        //}

        //static string SortThreeNumbers(int a, int b, int c)
        //{
        //    if (a > b && a > c) // max e A
        //    {
        //        return b > c ? $"{a},{b},{c}" : $"{a},{c},{b}";
        //    }

        //    if (b > a && b > c) // max e b
        //    {
        //        return c > a ? $"{b},{c},{a}" : $"{b},{a},{c}";
        //    }

        //    return b > a ? $"{c},{b},{a}" : $"{c},{a},{b}";
        }
    }
}
